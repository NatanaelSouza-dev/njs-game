// This patch is no longer needed.
// The client now uses @colyseus/sdk@0.18.2 which matches the server (0.18.5).
// consumeSeatReservation works natively — no monkey-patching required.
console.log('[patch_script] No patches needed for @colyseus/sdk@0.18.2');
